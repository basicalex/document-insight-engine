# Agent Memory for Project: document-insight-engine
This file contains persistent context, decisions, and knowledge for the AI agent.
Agents should read this to understand project history and append new decisions here.

## Core Decisions
- [2026-02-11 12:13] PRD intake executed in append mode for tag master using .taskmaster/docs/prd.txt; generated 15 normalized tasks (IDs 1-15) spanning foundation->ingestion->engine->API->frontend->validation with explicit dependency DAG and PRD links set at task level.
- [2026-02-11 12:16] Performed task-breakdown on master tasks 8,11,12,13,15: added 5 subtasks each with dependency chains for implementation order and test-first closure.
- [2026-02-11 12:18] Ran prd-align on master tasks: tightened Task 12 and Task 15 to explicitly enforce PRD metrics (Tier1 <2s, Tier4 >98% with character-level grounding), added Tier4 token-estimation/budget guard subtask (12.6), and aligned validation planning to 60/30/10 test pyramid.
- [2026-02-11 12:20] Applied tag-aligned workflow lanes: moved tasks from master into infra(1-2), ingestion(3-8), engine(9-12), api-ui(13-14), qa(15) to support lane-based execution and testing.
- [2026-02-11 13:34] Completed ingestion task 3: added upload intake service with MIME/size validation, filename sanitization, deterministic document IDs, idempotent file persistence under data/uploads, FastAPI /ingest endpoint, and pytest coverage for allowed/blocked MIME, traversal-safe names, oversized uploads, and idempotency.
- [2026-02-11 13:35] Completed ingestion task 4: added Tier1TextExtractor supporting PDF text-layer extraction via PyMuPDF with OCR fallback for scanned PDFs/images, page-level provenance metadata, and tests covering fallback triggering and page-order output semantics.
- [2026-02-11 13:47] Completed ingestion task 5: implemented Tier2DoclingParser with pluggable Docling backend, markdown export handling, heading-aware section paths, stable block IDs, and markdown table atomicity guarantees; added parser tests covering table atomic blocks, section lineage, deterministic IDs, and backend-provided page references.
- [2026-02-11 13:49] Completed ingestion task 6: implemented ParentChildChunker producing ~1024-token parent and ~256-token child chunks (configurable), preserving block order/lineage metadata (section path, page refs, block IDs), enforcing child->parent linkage IDs, and keeping table blocks atomic during child generation; added chunking tests for boundaries, linkage, and table unsplit behavior.
- [2026-02-12 10:59] Completed engine task 11 by adding src/engine/cloud_agent.py: strict allowlist for list_sections/read_section/keyword_grep with argument validation, deterministic loop state machine capped at 5 iterations, structured termination reasons, and trace logging for loop/tool activity; added run_agent helper and exports in src/engine/__init__.py; added tests/test_cloud_agent.py covering allowlist rejection, 5-loop cap, and successful tool->final answer path; full pytest passes (45/45).
- [2026-02-12 11:18] Backfilled engine completion history: Task 9 (src/engine/local_llm.py) implemented grounded local QA with retrieval-aware prompts, insufficient-evidence fallback, and trace metrics; Task 10 (src/tools/fs_tools.py) implemented deterministic markdown tools list_sections/read_section/keyword_grep with normalized keys, bounded outputs, and structured errors; Task 12 (src/engine/extractor.py) implemented Tier 4 structured extraction with schema enforcement, per-field character-level provenance checks, and token budget guards. Tests added: tests/test_local_llm.py, tests/test_fs_tools.py, tests/test_extractor.py; full suite passing.
- [2026-02-12 11:18] Documentation sync: updated README.md API status to reflect current endpoints (/healthz, /ingest), planned endpoints (/ask, /upload alias), and added implemented engine component summary for local QA, cloud agent loop guard, Tier4 extractor, and markdown FS tools.
- [2026-02-12 11:34] 2026-02-12: Completed task 13 (api-ui) by implementing /ingest and /ask contracts in src/api/main.py with correlation-id middleware, normalized error envelopes, timeout guardrails, and Idempotency-Key replay semantics; added tests/test_api.py covering mode routing, malformed payloads, ingest idempotency, and timeout handling; updated dependencies with python-multipart and dev httpx.
- [2026-02-12 11:58] Completed qa task 15 by adding tests/test_phase_validation.py covering fast ingest->ask latency budget (<2s), restart persistence with retained upload/index state, deep-mode 5-loop guard enforcement, and Tier 4 extraction accuracy>=98% with character-level grounding checks. Added CI-friendly metrics via pytest record_property; full suite passing (67).
- [2026-02-12 12:52] Integration task 17 progress: wired runtime /ingest path to run IngestionOrchestrator (extract->parse->chunk->embed->index) with timeout and idempotency replay; added best-effort runtime extractor/parser and deterministic hashing embedder; added assignment-compatible POST /upload supporting single and multi-file uploads with shared ingest semantics; API tests expanded and passing.
- [2026-02-12 12:57] Integration task 17 subtask 3 complete: API now persists per-document ingest responses in-memory, exposes GET /ingest/{document_id} for deterministic status lookup, and guards /ask with document_not_ready (409) when orchestrated ingest status is non-indexed.
- [2026-02-12 13:00] Integration task 17 progress update: added GET /ingest/{document_id} status lookup, /ask readiness gating for non-indexed orchestrated docs, Streamlit updates for /upload multi-file and status refresh, expanded frontend client tests for upload/status, added runtime pipeline tests, and published Docker manual verification checklist in README.
- [2026-02-12 13:05] Completed integration task 17: wired /ingest orchestration end-to-end, added /upload alias + batch support, persisted ingestion status with GET /ingest/{document_id}, updated Streamlit frontend for upload/status refresh, added deterministic hashing vectors and runtime pipeline fallbacks, introduced deep-mode capability flags (DEEP_MODE_ENABLED + CLOUD_AGENT_PROVIDER), expanded health diagnostics with dependency report, and hardened container/runtime deps (redisvl/pymupdf/pillow/pytesseract + tesseract-ocr in Dockerfile). Full test suite passing (81).
- [2026-02-12 19:50] Decision: During active development for document-insight-engine, use a hybrid workflow: run Redis + Ollama in Docker, run API + Streamlit locally with reload, and reserve full dockerized rebuilds for integration verification.
- [2026-02-12 19:52] Implemented docker-compose.dev.yml overlay for fast development: API runs with uvicorn --reload and bind mounts for src/frontend, UI uses Streamlit file watcher polling; use docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d api ui.
- [2026-02-13 06:41] Configured custom AOC Zellij layout 'aoc.hybrid' for hybrid development: panes for Agent, local API (uvicorn --reload), local UI (streamlit), Docker infra logs (redis+ollama), and dev shell with local env defaults.
- [2026-02-13 06:53] Updated aoc.hybrid Zellij layout to remove Explorer (Yazi) and Agent panes; hybrid now opens API Dev, UI Dev, Infra Logs, and Dev Shell only.
- [2026-02-13 07:03] Hybrid layout hardening: API/UI panes now auto-bootstrap .venv, install missing deps (.[dev,ui]), use python -m streamlit/uvicorn, and prefer python3.12 for venv creation to avoid Python 3.14 incompatibility with redisvl.
- [2026-02-13 07:08] Streamlit first-run onboarding prompt can block UI startup in hybrid layout; mitigation applied in aoc.hybrid UI pane to seed ~/.streamlit/config.toml with browser.gatherUsageStats=false before launching streamlit.
- [2026-02-13 07:22] Hybrid layout timeout fix: set INGEST_TIMEOUT_SECONDS=300 alongside REQUEST_TIMEOUT_SECONDS=300 in API/Dev Shell panes to satisfy Settings validation constraint request_timeout_seconds <= ingest_timeout_seconds.
- [2026-02-13 07:38] POC performance tuning: switched default local model to llama3.2:1b, set compose API env LOCAL_LLM_MODEL=llama3.2:1b, and updated hybrid layout to export LOCAL_LLM_MODEL defaulting to llama3.2:1b. Pulled model in Ollama and confirmed first stream token latency improved (~126s -> ~45s).
- [2026-02-13 10:54] 2026-02-13: Added project-shared layout support via .aoc/layouts across aoc-layout, aoc-launch, aoc-new-tab, and aoc-control; named layout resolution now prefers project layouts before ~/.config/zellij/layouts.
- [2026-02-13 11:34] 2026-02-13: Completed completion task 20 by introducing pluggable API state persistence (Redis-first with in-memory fallback). Added schema-versioned keyspace for idempotency replays/claims, ingestion status records, and session histories with TTL controls, plus API wiring for conflict-safe idempotency and restart-safe state retrieval.
- [2026-02-13 11:40] 2026-02-13: Completed completion task 21 (index backend policy). API now enforces fail-fast startup when Redis/RedisVL index bootstrap fails unless ALLOW_IN_MEMORY_INDEX_FALLBACK=true in local/dev. Added explicit readiness diagnostics (healthz readiness payload + /readyz 200 ready vs 503 degraded) to surface Redis-backed vs fallback index state.
- [2026-02-13 13:07] Task 26 completed with built-in observability registry (no external metrics dependency): /metrics exposes Prometheus-style counters/gauges, /healthz includes SLO evaluation snapshot, ingestion queue retry/dead-letter and QA citation completeness are instrumented, and evaluation gating is implemented via src/evals/harness.py + curated fixtures in tests/data/eval and CI workflow.
- [2026-02-13 18:11] Default local runtime profile now uses Ollama for deep mode and both local/cloud embeddings; Gemini remains opt-in via CLOUD_AGENT_API_KEY and provider flags.
