- [2026-02-13 11:34] Implemented task 20 state persistence: added src/api/state_store.py (Redis + in-memory adapters), wired src/api/main.py to store/retrieve idempotency, ingestion status, and session turns via state store, added settings for backend/key prefix/TTLs/max turns, and expanded tests for replay conflicts plus restart persistence. Targeted tests passed: pytest tests/test_api_state_store.py tests/test_api.py tests/test_settings.py and tests/test_phase_validation.py::test_phase_restart_retains_upload_and_index_state.
- [2026-02-13 11:40] Completed task 21: added allow_in_memory_index_fallback setting with local/dev-only guard; refactored API index initialization into policy-aware builder; fail-fast when Redis index unavailable and fallback disabled; explicit degraded fallback path when gate enabled; health diagnostics now include readiness and new /readyz endpoint. Added tests for prod fail-fast, dev fallback, readyz degraded response, and settings validation. Targeted tests passed (43 + 3).
- [2026-02-13 12:05] Completed Tasks 22-24. Added optional AI dependency packaging and capability diagnostics, parser chain routing with Google adapter and parser attribution events, and new /extract API workflow with artifact persistence and integration tests. Next pending task is 25 (async ingestion worker queue/progress model).
- [2026-02-13 13:07] Completed Task 26 observability/eval suite: added src/api/telemetry.py with SLO checks + Prometheus metrics, wired API middleware and /metrics endpoint with correlation-to-trace linkage, instrumented ingestion queue retries/dead letters and chat citation completeness, added eval harness at src/evals/harness.py with curated corpus fixtures/tests, and CI workflow gate .github/workflows/ci.yml. Verified with pytest tests/test_api.py tests/test_settings.py tests/test_evaluation_harness.py tests/test_phase_validation.py and python -m src.evals.harness --assert-thresholds.
- [2026-02-13 13:42] Task 27 completed: Streamlit UI now includes structured extraction controls/results, runtime readiness banner based on /healthz, and observability panel rendering /healthz observability + /metrics text. Added frontend client methods for /extract /healthz /metrics, expanded frontend tests, and set compose defaults to local deep profile (DEEP_MODE_ENABLED=true, CLOUD_AGENT_PROVIDER=local).
- [2026-02-13 13:44] Handoff
Objective: complete UI parity for full feature access and maintain production readiness observability/eval coverage.
Task status: Task 27 complete (all subtasks done); prior tasks 18-26 done.
Done: added Streamlit structured extraction workflow, runtime readiness banner, observability panel; added frontend client methods for /extract /healthz /metrics; added session state defaults for runtime/metrics/extract; set compose defaults to DEEP_MODE_ENABLED=true + CLOUD_AGENT_PROVIDER=local; updated README coverage/setup docs; updated tests for frontend client/state and compose; all targeted and regression tests passing.
In progress: none.
Blocked: none.
Files touched (latest batch): frontend/app.py, frontend/client.py, frontend/state.py, docker-compose.yml, docker-compose.dev.yml, README.md, tests/test_frontend_client.py, tests/test_frontend_state.py, tests/test_compose.py.
Command outcomes: pytest frontend suite 17 passed; pytest regression suite 73 passed.
Open decisions/assumptions: local deep provider remains default in compose for out-of-box full-feature UX; Gemini path still supported via env override + API key.
Next steps: 1) optionally commit UI/compose/docs batch, 2) optionally run full test suite, 3) optionally add true deep token streaming and richer metrics visualization cards.
