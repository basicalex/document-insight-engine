- [2026-02-15 17:23] Implemented parsed markdown artifact persistence in BestEffortParser and wired API parser with parsed_dir; hardened CloudAgentEngine to return deterministic insufficient-evidence responses when fs tool bootstrap fails (parsed_artifact_missing/tooling_unavailable); added runtime_pipeline and cloud_agent tests; full pytest passing (164).
- [2026-02-17 15:23] Handoff objective: make document-insight-engine directly functional in both local hybrid (local API/UI + docker infra) and fully dockerized runs with a single profile-driven workflow. Status: planning complete, implementation pending.
- [2026-02-17 15:23] Handoff plan: 1) Define two env profiles (lite default, full opt-in) in repo and unify local+docker env loading. 2) Update hybrid layout bootstrap to install profile-matched extras (lite=.[dev,ui,ai-lite], full=.[dev,ui,ai]). 3) Align compose/hybrid runtime flags (DOCLING/GOOGLE/LANGEXTRACT/PARSER_ROUTING/DEEP_MODE/PROVIDER/EMBEDDINGS). 4) Add GOOGLE_API_KEY alias support for CLOUD_AGENT_API_KEY in settings/readiness checks. 5) Improve UI readiness banner to separate blocking vs optional warnings. 6) Add project-scoped .aoc/layouts hybrid template and startup scripts. 7) Add tests for settings alias, compose profile contracts, and profile smoke. 8) Validate both profiles with upload->ingest->ask fast/deep and parsed artifact persistence.
- [2026-02-17 15:23] Open decisions and assumptions: default profile should be lite; full profile requires docling install and optional cloud key. Risks: env drift between layout and compose, port conflicts if docker api/ui left running, and misleading readiness messaging if optional capabilities are enabled without deps. Immediate next 5 steps: create profile env files, patch compose to consume profile vars, patch hybrid layout bootstrap/env, patch settings+health alias logic, run pytest + docker/manual smoke and archive STM.
